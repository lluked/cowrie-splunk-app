name: Call splunk-app-inspect

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  call-workflow:
    uses: lluked/Splunk-App-Testing/.github/workflows/splunk-app-inspect.yml@main
